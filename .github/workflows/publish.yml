name: Publish to NPM

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Bun
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest

    - name: Install dependencies
      run: bun install

    - name: Setup NPM registry
      run: bunx npm-registry-auth-cli setup --registry=https://registry.npmjs.org --token=${{ secrets.NPM_TOKEN }}

    - name: Publish to NPM
      run: bun publish
